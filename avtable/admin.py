from django.contrib import admin
#from .models import Mavito
# Register your models here.
#class MavitoAdmin(admin.ModelAdmin):
#	list_display = ("id", "obj", "addr", "price", "date", "last_updated",)
#	prepopulated_fields = {"slug": ("flat",)}
#admin.site.register(Mavito, MavitoAdmin)